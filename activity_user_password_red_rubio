user=input("Enter Username: ")
password=input("Enter password: ")

if (len(user)>=8):
    if(user=="andresaa"):
        if(len(password)>=8):
            if(password=="pogiako2"):
                print("Login Success")
            else:
                print("Error")
        else:
            (len(password)>8) | (len(password)<8)
            print("Error")
    else:
        print("Error")
else:
    (len(user)>8) | (len(user)<8)
    print("Error")
